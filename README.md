# SISROAD Dash

## Rodar (Codespaces / Linux)

1) Criar venv:
   python -m venv .venv
2) Ativar:
   source .venv/bin/activate
3) Instalar deps:
   pip install -r requirements.txt
4) Configurar env:
   cp .env.example .env
5) Rodar:
   python run.py
